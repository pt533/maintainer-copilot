Title: Support project-specific triage rules

I would like to configure custom heuristics for my repository.

Requested behavior:
- allow custom label mappings
- allow repository-specific required fields
- allow custom missing-information checks

Why this matters:
Different projects use different labels and triage rules, so a shared default heuristic is not always enough.

Possible direction:
A simple config file in the repository root would be enough for an initial version.
